import React from "react";
import "./leadSelect.css";

const LeadSelect = ({ name }) => {
  return (
    <div className="teacherSelectCard">
      <h1>{name}</h1>
    </div>
  );
};

export default LeadSelect;
